<% if (old || interface) { -%>
import { Document } from 'mongoose';

export interface I<%= nameClass %> {
  example_1: string;
  example_2: number;
  isDeleted?: boolean;
}

export type <%= nameClass %>Document = Document & I<%= nameClass %>;
<% } %>